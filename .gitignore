# .NET
bin/
obj/
[Dd]ebug/
[Rr]elease/
*.user
*.suo
.vs/
artifacts/
TestResults/
*.trx
*.coverage
coverage*.xml

# Rider / VS Code
.idea/
.vscode/*
!.vscode/launch.json
!.vscode/tasks.json

# Python (tools-sidecar, betikler)
__pycache__/

# `pip install -e` bunu depo icinde uretiyor.
*.egg-info/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/

# Uygulama verisi — asla depoya girmez
#
# HEPSI DEPO KOKUNE ANKRAJLI (basinda /) ve bu bir uslup tercihi
# degil: ankrajsiz "storage/" deseni `src/.../Storage/` KAYNAK
# klasorunu de yutuyor. Windows'ta ignore eslesmesi buyuk/kucuk harf
# duyarsiz oldugu icin `Storage` ile `storage` ayni sayiliyor.
#
# BU HATA IKI KEZ OLDU. Ilkinde `FileSystemAssetStore.cs` hic commit
# edilmedi; asagiya ankrajli desenler eklendi ama ANKRAJSIZ OLANLAR
# SILINMEDI, yani duzeltme yarim kaldi. Ikincisinde ayni desen uc
# dosyayi birden yuttu (`S3AssetStore`, `StorageSelection`,
# `RetentionPolicy`) ve `git add -A` tek kelime etmedi -- yok sayilan
# dosyalar sessizce atlaniyor. Ikisini de CI yakaladi.
# URETILEN KLASORLER ADIYLA YAZILIYOR, desenle degil.
#
# Ankrajsiz `storage/` kaynak klasorunu yutuyordu; ankrajli
# `/storage/` ise host'larin KENDI calisma dizinlerine yazdigi
# `src/<proje>/storage/` klasorlerini kacirdi ve uretilen medya
# dosyalari commit'e girdi. Iki ucu da olcup gorduk.
#
# `src/*/storage/` gibi bir desen ikisini de cozmuyor: Windows'ta
# eslesme buyuk/kucuk harf duyarsiz oldugu icin `Persistence/Storage/`
# yine yutulurdu. Adiyla yazmak uzun ama BELIRSIZ DEGIL.
/data/
/storage/
/assets-store/
/output/
/src/BytemountsAiStudio.Worker/storage/
/src/BytemountsAiStudio.Worker/output/
/src/BytemountsAiStudio.Api/storage/
/src/BytemountsAiStudio.Api/output/
/src/BytemountsAiStudio.Cli/storage/
/src/BytemountsAiStudio.Cli/output/
*.mp4
*.wav
*.mp3
*.partial.*

# Sırlar
*.env
.env.*
appsettings.Local.json
appsettings.*.Local.json
secrets/
*.pem
*.key
client_secret*.json
token*.json

# Üretilen pano — plandan türetilir, depoda tutulur ama elle düzenlenmez
# (kasten yok sayılmıyor; ekip dışı biri de görebilsin)

# İşletim sistemi
Thumbs.db
.DS_Store
desktop.ini

# (Uretilen ciktilar yukarida, ankrajli olarak tanimli.)
